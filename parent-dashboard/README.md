# TeachBox Parent Dashboard

The parent dashboard should consume the stable contracts under:

- `GET /api/v1/parent/sessions`
- `GET /api/v1/parent/sessions/:sessionId`
- `GET /api/v1/lessons`

Do not couple the dashboard directly to provider internals.
